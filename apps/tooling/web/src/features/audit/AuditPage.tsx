// Placeholder — enveloped /audit table with filters + paging lands in a later
// step (admin sees all; other roles are scoped to their own actions).
export function AuditPage() {
  return (
    <div className="space-y-3">
      <h1 className="text-xl font-semibold">Audit</h1>
      <p className="text-neutral-500">Audit log view coming in a later step.</p>
    </div>
  );
}
