# docs/mockups

Static HTML design references. **Not** the running React app.

## Locked — Pot Map (use this)

| File | Role |
|---|---|
| **`potmap-current-product.html`** | **LOCKED** visual for machine Pot Map (2026-07-27). Matches React Phases 1–3. |

**Spec:** [`docs/13-potmap-ui.md`](../13-potmap-ui.md)

Open in a browser to share the picture of the product. Do not reintroduce deleted
historical potmap HTML as authority.

## Other mockups (not pot-map authority)

| File | Role |
|---|---|
| `tool-census.html` / `.tpl.html` | Tool census / inventory worksheets (separate feature) |
| `tool-worksheet.html` / `.tpl.html` | Tool worksheet templates (separate feature) |

These may inform future Tools/census UI; they **do not** override pot-map layout.

## Deleted (superseded — do not restore as source of truth)

- `potmap-lg1000ap.html` — early ring + full offset dump  
- `potmap-lg1000ap-20260724.html` — ring + **full** 55-station crib  
- `potmap-ui-ab-compare.html` — A/B selection experiments  

Rationale: full-crib-on-machine-view and pot-1-at-12 / orange hub were retired by
the locked product decisions. History lives in git if needed.
